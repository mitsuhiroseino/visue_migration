import { IMAGE_MANIPULATION_TYPE } from '../constants';

/**
 * 画像操作`CreateDirectories`の設定
 */
export type CreateDirectoriesConfig = {
  /**
   * 画像操作種別
   */
  type: typeof IMAGE_MANIPULATION_TYPE.CREATE_DIRECTORIES;
};
